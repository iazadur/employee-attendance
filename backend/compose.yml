services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "3001:3001"
    env_file:
      - .env
    environment:
      # Set true only for local demo/dev (seed wipes & recreates demo data)
      RUN_SEED: "true"

